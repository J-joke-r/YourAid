# YourAid
A simple medical web app using Flask that collects patient data through an HTML form and stores it in an Excel (.xlsx) file using Pandas. Ideal for small-scale clinics or academic projects needing lightweight, local data logging without a full database setup.
